# Backend (Vercel Python function / api/app) — server-side only, never expose these to the browser.
SUPABASE_URL=https://kpdehawhhldbjzgkfiff.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Frontend (web/.env) — safe to expose; used only to call the Supabase Edge
# Function `extract-approval` directly from the browser, same as before.
VITE_SUPABASE_URL=https://kpdehawhhldbjzgkfiff.supabase.co
VITE_SUPABASE_ANON_KEY=

# Frontend: base path for the backend API. Leave empty in production (same
# origin on Vercel); set to http://localhost:8000 for local dev.
VITE_API_BASE=
